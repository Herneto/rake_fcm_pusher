<div class="grid-container">
    <%= form_tag "/load", multipart: true do %>
        <%= file_field_tag :file %>
        <%= submit_tag "Enviar CSV"%>
    <% end %>
</div>
<div class="grid-container">
    <table>
    <thead>
    <tr>
        <th>Usuário</th>
        <th>Email</th>
    </tr>
    </thead>
    <tbody>
        <% @users.each do |user|%>
            <tr>
                <td><%= user.username %></td>
                <td><%= user.email %></td>
            </tr>
        <% end %>
    </tbody>
    </table>
</div>

